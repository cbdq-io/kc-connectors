name: AzureServiceBusSinkConnector
description: Deploy Azure Service Bus for testing.
runtime:
  name: python
  options:
    toolchain: pip
    # virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
