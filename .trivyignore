# The following vulnerabilities are found in the base image (confluentinc/cp-kafka-connect:8.3.1).
#
#    trivy image --format json confluentinc/cp-kafka-connect:8.3.1 \
#    | jq -r '.Results[] | select(.Vulnerabilities != null) | .Vulnerabilities[] | .VulnerabilityID' \
#    | sort -u
#
